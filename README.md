Challenge1: Restart nginx service when it crashes
- Hint: Use `subprocess` to restart nginx automatically.

Challenge2: Monitor disk usage and send alerts via Telegram
- Hint: Use `psutil` to check disk usage